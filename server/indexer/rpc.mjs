export function boundedRPC(url,deadline){return {async send(method,params){const remaining=deadline-Date.now();if(remaining<=0)throw Error('BUDGET');try{const r=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({jsonrpc:'2.0',id:1,method,params}),signal:AbortSignal.timeout(Math.min(4000,remaining))});const data=await r.json();if(!r.ok||data.error)throw Object.assign(Error('RPC rejected'),{code:data.error?.code,status:r.status,info:{error:data.error,responseStatus:r.status}});return data.result;}catch(e){if(Date.now()>=deadline)throw Error('BUDGET');throw e;}}};}
